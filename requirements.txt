Django>=5.0,<6.0
psycopg2-binary>=2.9
redis>=5.0
celery>=5.3
django-celery-beat>=2.6
channels>=4.0
channels-redis>=4.2
# Fixado na versão mais recente e mantida (linha 2.x). O >=2.1 antigo ficava
# preso na 2.1.5 (não força upgrade) — versão de 2023 com login/story antigos.
# Bump deliberado quando for testar uma release nova.
instagrapi==2.18.12
# MoviePy REMOVIDO: instagrapi 2.18 exige Pillow>=12.2, e o MoviePy trava em
# Pillow<12 (conflito insolúvel). A thumbnail de vídeo agora é gerada por
# ffmpeg em engine/media_cleaner.extrair_thumbnail e passada pronta no upload.
cryptography>=42.0
pywebpush>=2.0.0
django-environ>=0.11
gunicorn>=22.0
whitenoise>=6.6
Pillow>=12.2.0,<13
django-htmx>=1.17
requests>=2.31.0
